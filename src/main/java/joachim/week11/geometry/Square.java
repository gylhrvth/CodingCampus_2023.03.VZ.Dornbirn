package joachim.week11.geometry;

public class Square extends Rectangle {

    public Square(double length) {
        super(length,length);
        this.length = length;
        this.width = length;
    }
}
